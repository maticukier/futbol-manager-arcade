import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maticukier.futbolmanagerarcade',
  appName: 'Futbol Manager Arcade',
  webDir: 'dist',
  android: {
    backgroundColor: '#0b1220',
  },
  ios: {
    backgroundColor: '#0b1220',
  },
};

export default config;
